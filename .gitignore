## .NET
bin/
obj/
*.user
Directory.Build.props.user

## Node (changesets tooling only — see docs/RELEASE-STRATEGY.md)
node_modules/

## IDE
.vs/
.vscode/*
!.vscode/extensions.json
*.suo

## Test / temp artifacts
TestResults/
*.log
tmp/
temp/

## OS
Thumbs.db
.DS_Store
# Squad: ignore runtime state (logs, inbox, sessions)
# Note: .squad/casting/* is identity and MUST be committed to main, not ignored
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
.squad/.cache/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Build/packaging output
artifacts/

## MkDocs (gh-pages) local build output
gh-pages/_site/
gh-pages/.cache/
gh-pages/.venv/
gh-pages/.venv-*/

# Generated by the deploy workflow (scripts/Update-StarHistory.ps1) and downloaded
# into the docs before the site build; hooks.py writes a placeholder locally.
gh-pages/docs/assets/images/star-history.svg
