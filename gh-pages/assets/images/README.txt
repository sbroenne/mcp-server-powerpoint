This directory needs a real `icon.png` (recommend 512x512 or 256x256, PowerPoint-themed,
matching the brand color #B7472A used in assets/css/style.css) plus a `favicon.ico` derived
from it. Pages reference `/assets/images/icon.png` already — drop the real file in here with
that exact name and no other changes are needed.

TODO(sbroenne): add real icon.png + favicon.ico before going live.
