---
"powerpointmcp": minor
---

Add `customshow` tool for named custom slide shows: list, create, and delete curated, ordered subsets of a presentation's slides, so one deck can be reused for different audiences without duplicating slides.
