---
"powerpointmcp": minor
---

Add editable WordArt creation and independent 3D shape rotation controls to both the MCP server and CLI. 3D rotations can update individual axes without changing the shape's existing 2D rotation.
